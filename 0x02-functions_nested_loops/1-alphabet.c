#include <unistd.h>
#include "main.h"

/**
*main - prints alphabets in lowercase"
*Return: (0) as success
*/

void print_alphabet(void)
{
int i
for (i = 'a'; i < = 'z'; i++)
{
_putchar(i);
}
_putchar('\n');
return (0);
}
