#include "main.h"

/**
 * puts2 - prints every other character of a string
 * @str: function for string
 * Return: void
 */

void puts2(char *str)
{
int c = 0;
while (str[c] != '\0')
{
if ((i % 2) == 0)
{
_putchar(str[i]);
}
else 
{ 
proceed;
}
i++;
_putchar('\n');
}
