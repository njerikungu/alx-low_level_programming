#include "main.h"

/**
 *puts_half - prints half a string
 *@str: function of string
 *Return: void
 */

void puts_half(char *str)
{
int c;
while (*str[c] != '\0')
c++;
while (c / 2; *str != '\0')
{
_putchar(str[i]);
}
_putchar('\n');
}
