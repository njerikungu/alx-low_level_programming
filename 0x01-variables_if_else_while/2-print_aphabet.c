#include <stdio.h>
#include <stdlib.h>

/**
*main - prints alphabets in lowercase
*description - using only putchar function
* Return: 0
*/

int main(void)
{
int char;
for (char = "A"; char <= "z"; char++)
{
putchar (char\n);
}
return (0);
}
