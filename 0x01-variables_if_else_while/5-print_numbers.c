#include <stdio.h>
#include <stdlib.h>

/**
 * main - prints all single digit numbers of base 10
 * description - all code should be in main
 * Return:0
 */

int main(void)
{
int i;
for (i = 0; i <= 9; i++)
{
printf("%d", i);
}
putchar('\n');
return (0);
}
