#include <stdio.h>
#include <stdlib.h>

/**
*main - prints alphabets in lowercase
*description - using only putchar function
* Return: 0
*/

int main(void)
{
int s;
for (s = "A"; s <= "z"; s++)
{
putchar (s\n);
}
return (0);
}
