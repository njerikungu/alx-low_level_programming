#include <stdio.h>

/**
 * main- string
 * Description- Prints exactly as it is
 * Returns 0
 */

int main(void)
{
printf("%s", "with proper grammar, but the outcome is a piece of art,\n");
return (0);
}
